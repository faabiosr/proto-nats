// Code generated by protoc-gen-nats {{ .Version }}, DO NOT EDIT.
// source: {{ .File }}

/*
Package {{ .Package }} is a generated proto-nats stub package.
This code was generated with github.com/faabiosr/proto-nats/protoc-gen-nats {{ .Version }}.

It is generated from these files:
	{{ .File }}
*/
package {{ .Package }}

import (
	"context"
	"errors"
	"time"

	pn "github.com/faabiosr/proto-nats/proto"
	"github.com/golang/protobuf/ptypes"
	nats "github.com/nats-io/nats.go"
)

const (
{{- range .Services }}
    {{- $svcName := .Name }}
	{{- $svcOption := .Options | subjectPrefix | deref }}
	{{- range .Method }}
	{{- $methodOption := .Options | subject | deref }}
	{{- $prefixName := $svcName | deref | snakecase | replace "_" "." }}
	{{- $sufixName := .Name | lower }}
	{{ $svcName }}{{ .Name }}Subject = "{{ default $prefixName $svcOption }}.{{ default $sufixName $methodOption }}"
	{{- end }}
{{- end }}
)

{{- range .Services}}
    {{- $svcName := .Name | deref }}
// {{ $svcName }} Interface
type {{ $svcName }} interface {
    {{- range .Method }}
	{{ .Name  }}(context.Context, *{{ .InputType | splitList "." | last }}) (*{{ .OutputType | splitList "." | last }}, error)
	{{- end }}
}

    {{- $sub := printf "%s%s" $svcName "EncodedSubscriber" }}
	{{- $subStruct := $sub | unexported }}
// {{ $svcName }} Encoded Subscriber
type {{ $subStruct }} struct {
	enc       *nats.EncodedConn
	queueName string
	svc       {{ $svcName }}
	subs      []*nats.Subscription
}

// New{{ $sub }} creates a {{ $subStruct }} that implements the QueueSubscriber.
func New{{ $sub }}(enc *nats.EncodedConn, queueName string, svc {{ $svcName }}) pn.QueueSubscriber {
	return &{{ $subStruct }}{enc, queueName, svc, []*nats.Subscription{}}
}

// Subscribe will register the services in encoded queue broker.
func (s *{{ $subStruct }}) Subscribe() error {
{{ range .Method }}	if err := s.register(s.enc.QueueSubscribe({{ $svcName }}{{ .Name }}Subject, s.queueName, s.{{ .Name | lower }})); err != nil {
		return err
	}

{{ end }}	return nil
}

// Unsubscribe will unregister the services from the encoded queue broker.
func (s *{{ $subStruct }}) Unsubscribe() error {
	if len(s.subs) == 0 {
		return nil
	}

	for _, sub := range s.subs {
		if err := sub.Unsubscribe(); err != nil {
			return err
		}
	}

	return nil
}

func (s *{{ $subStruct }}) register(sub *nats.Subscription, err error) error {
	if err != nil {
		return err
	}

	s.subs = append(s.subs, sub)

	return nil
}
{{- end}}

{{ range .Services }}{{ $svc := .Name | deref }}{{ $rep := $svc | len | add 10 | int }}
// {{ repeat $rep "=" }}{{ $encSub := printf "%s%s" $svc "EncodedSubscriber" }}

{{ range .Method }}
func (s *{{ $encSub | unexported }}) {{ .Name | lower }}(subject, reply string, req *{{ .InputType | splitList "." | last }}) {
	res, err := s.svc.{{ .Name }}(context.Background(), req)

	if len(reply) == 0 {
		return
	}

	if err != nil {
		s.enc.Publish(reply, &pn.EncodedMessage{Error: err.Error()})
		return
	}

	msg := &pn.EncodedMessage{}
	msg.Body, err = ptypes.MarshalAny(res)

	if err != nil {
		msg.Error = err.Error()
	}

	s.enc.Publish(reply, msg)
}{{ end }}
{{ $rep := .Name | len | add 15 | int }}
// {{ repeat $rep "=" }}
// {{ .Name }} Encoded Client
// {{ repeat $rep "=" }}{{ $encCli := printf "%s%s" $svc "EncodedClient" }}
type {{ $encCli | unexported }} struct {
	enc     *nats.EncodedConn
	timeout time.Duration
}

// New{{ $encCli }} creates a {{ $encCli | unexported }} thtat implements the {{ .Name }}.
func New{{ $encCli }}(enc *nats.EncodedConn, timeout time.Duration) {{ .Name }} {
	return &{{ $encCli | unexported }}{enc, timeout}
}
{{ range .Method }}
func (c *{{ $encCli | unexported }}) {{ .Name }}(ctx context.Context, req *{{ .InputType | splitList "." | last }}) (*{{ .OutputType | splitList "." | last }}, error) {
	var msg pn.EncodedMessage

	if err := c.enc.Request({{ $svc }}{{ .Name }}Subject, req, &msg, c.timeout); err != nil {
		return nil, err
	}

	if err := msg.GetError(); len(err) != 0 {
		return nil, errors.New(err)
	}

	res := &{{ .OutputType | splitList "." | last }}{}

	if err := ptypes.UnmarshalAny(msg.GetBody(), res); err != nil {
		return nil, err
	}

	return res, nil
}
{{ end }}{{ end }}
